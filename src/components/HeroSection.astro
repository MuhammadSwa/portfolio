---
import Tooth from '../pics/grinning-face.svg'
import Mirror from '../pics/mirror.svg'
---
<section
  class="relative min-h-screen flex items-center bg-slate-950 overflow-hidden py-12 sm:py-16 lg:py-10"
>
  <!-- Subtle Background Texture -->
  <div class="absolute inset-0 z-0">
    <div
      class="absolute top-[-10%] right-[-5%] w-100 sm:w-150 h-100 sm:h-150 bg-teal-900/10 rounded-full blur-[120px] mix-blend-screen animate-pulse-slow"
    >
    </div>
    <div
      class="absolute bottom-[-10%] left-[-10%] w-[350px] sm:w-[500px] h-[350px] sm:h-[500px] bg-indigo-900/10 rounded-full blur-[100px] mix-blend-screen animate-pulse-slow delay-700"
    >
    </div>
    
    <!-- MOBILE ONLY: Floating Props Background -->
    <div class="lg:hidden absolute inset-0 overflow-hidden pointer-events-none opacity-20">
        <!-- Mirror tool floating behind text -->
        <div class="absolute top-[20%] right-[20%] animate-float" style="animation-delay: 0s;">
            <Mirror class="w-24 h-24 text-teal-400 rotate-12"/>
        </div>
        
        <!-- Large Sparkle -->
        <div class="absolute bottom-[10%] left-[15%] animate-float" style="animation-delay: 1s;">
            <svg class="w-16 h-16 text-indigo-400" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 0L14.59 9.41L24 12L14.59 14.59L12 24L9.41 14.59L0 12L9.41 9.41L12 0Z"/>
            </svg>
        </div>

        <!-- Small Sparkle -->
        <div class="absolute top-[40%] left-[10%] animate-float" style="animation-delay: 2s;">
            <svg class="w-8 h-8 text-teal-300" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 0L14.59 9.41L24 12L14.59 14.59L12 24L9.41 14.59L0 12L9.41 9.41L12 0Z"/>
            </svg>
        </div>
    </div>

    <div
      class="absolute inset-0 bg-[url('/noise.png')] opacity-[0.02] mix-blend-overlay"
    >
    </div>
  </div>

  <div class="max-w-6xl mx-auto px-5 sm:px-8 md:px-12 lg:px-16 relative z-10 w-full">
    <div class="grid lg:grid-cols-2 gap-8 sm:gap-10 lg:gap-20 items-center">
      

      <!-- Personal Introduction -->
      <div
        class="flex flex-col items-center text-center lg:items-start lg:text-left"
      >
        <div
          class="mb-4 sm:mb-6 inline-flex items-center gap-2 px-3 sm:px-4 py-2 rounded-full bg-slate-900/50 border border-slate-800/50 backdrop-blur-sm animate-fade-in opacity-0"
          style="animation-delay: 300ms;"
        >
          <span class="relative flex h-2 w-2">
            <span
              class="animate-ping absolute inline-flex h-full w-full rounded-full bg-teal-400 opacity-75"
            ></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-teal-500"
            ></span>
          </span>
          <span class="text-xs sm:text-sm text-slate-300 font-medium"
            >Welcome to my portfolio</span
          >
        </div>

        <h1
          class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-serif text-slate-100 leading-[1.1] mb-4 sm:mb-6 animate-fade-in opacity-0"
          style="animation-delay: 400ms;"
        >
          Hi, I'm <br />
          <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-teal-300 to-indigo-300"
            >Muhammad</span
          >
        </h1>

        <p
          class="text-base sm:text-lg md:text-xl text-slate-200 leading-relaxed max-w-xl mb-6 sm:mb-8 lg:mb-10 px-2 sm:px-0 animate-fade-in opacity-0"
          style="animation-delay: 500ms;"
        >
          A dental student from Egypt, documenting my clinical cases as I learn, practice, and refine my skills.
        </p>

        <!-- Mobile Quote - Shows on smallest screens only -->
        <p
          class="sm:hidden text-sm text-teal-200/80 font-handwriting italic mb-8 animate-fade-in opacity-0"
          style="animation-delay: 600ms;"
        >
          "Learning dentistry, one case at a time."
        </p>

        <div
          class="flex flex-col sm:flex-row gap-3 sm:gap-4 w-full sm:w-auto animate-fade-in opacity-0"
          style="animation-delay: 700ms;"
        >
          <a
            href="#cases"
            class="inline-flex items-center justify-center px-6 sm:px-8 py-3 sm:py-4 bg-teal-600 hover:bg-teal-500 active:bg-teal-700 text-white text-sm sm:text-base rounded-2xl transition-all duration-300 shadow-lg shadow-teal-900/20 group touch-manipulation"
          >
            <span>Explore My Cases</span>
            <svg
              class="w-4 h-4 sm:w-5 sm:h-5 ml-2 transition-transform group-hover:translate-x-1"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </a>
          <a
            href="#about"
            class="inline-flex items-center justify-center px-6 sm:px-8 py-3 sm:py-4 bg-slate-900/50 hover:bg-slate-800 active:bg-slate-900 text-slate-200 text-sm sm:text-base border border-slate-800 rounded-2xl transition-all duration-300 touch-manipulation"
          >
            About Me
          </a>
        </div>
      </div>

      <!-- Hero Visual - Hidden on Mobile, Flex on Desktop -->
      <div
        class="hidden lg:flex justify-center relative animate-fade-in opacity-0"
        style="animation-delay: 200ms;"
      >
        <div
          class="relative w-64 h-64 sm:w-80 sm:h-80 md:w-96 md:h-96 lg:w-[450px] lg:h-[450px]"
        >
          <!-- Decorative Rings -->
          <div
            class="absolute inset-0 border-2 border-slate-800 rounded-full scale-[1.02] animate-spin-slow-reverse opacity-30"
          >
          </div>
          <div
            class="absolute inset-0 border border-dashed border-teal-800/30 rounded-full scale-[1.1] animate-spin-slow"
          >
          </div>

          <!-- Dental Visual Container -->
          <div
            class="absolute inset-0 bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 rounded-full overflow-hidden shadow-2xl border-4 border-slate-800/50 group"
          >
            <!-- Subtle gradient glow -->
            <div class="absolute inset-0 bg-gradient-to-tr from-teal-900/20 via-transparent to-indigo-900/20">
            </div>

            <!-- Central Tooth Icon -->
            <div class="absolute inset-0 flex items-center justify-center">
              <Tooth class="w-28 h-40 sm:w-36 sm:h-48 md:w-40 md:h-56 lg:w-48 lg:h-64 drop-shadow-[0_0_30px_rgba(45,212,191,0.3)] group-hover:scale-105 transition-transform duration-500" />
            </div>

            <!-- Floating dental tool icons (Desktop) -->
            <div class="absolute inset-0">
              <div class="absolute top-[20%] left-[15%] animate-float" style="animation-delay: 0s;">
                <Mirror class="w-10 h-10 text-teal-400/60"/>
              </div>
              
              <div class="absolute top-[20%] right-[18%] animate-float" style="animation-delay: 1s;">
                <svg class="w-8 h-8 text-indigo-400/50" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 0L14.59 9.41L24 12L14.59 14.59L12 24L9.41 14.59L0 12L9.41 9.41L12 0Z"/>
                </svg>
              </div>
              
              <div class="absolute bottom-[25%] left-[20%] animate-float" style="animation-delay: 2s;">
                <svg class="w-5 h-5 text-teal-300/40" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 0L14.59 9.41L24 12L14.59 14.59L12 24L9.41 14.59L0 12L9.41 9.41L12 0Z"/>
                </svg>
              </div>
            </div>

            <!-- Subtle particle effect -->
            <div class="absolute inset-0 animate-[spin_20s_linear_infinite] opacity-30">
              <div class="absolute top-[30%] left-[50%] w-1 h-1 bg-teal-300 rounded-full"></div>
              <div class="absolute top-[60%] left-[30%] w-1 h-1 bg-indigo-300 rounded-full"></div>
              <div class="absolute top-[40%] right-[30%] w-1 h-1 bg-teal-200 rounded-full"></div>
            </div>
          </div>

          <!-- Floating "Signature" Note -->
          <div
            class="absolute -bottom-6 -left-8 bg-slate-900/90 backdrop-blur-md border border-slate-800 p-4 rounded-xl shadow-xl max-w-[200px] animate-float"
          >
            <p class="font-handwriting text-teal-200 text-sm leading-snug">
              "Learning dentistry, one case at a time."
            </p>
          </div>
        </div>
      </div>

    </div>
  </div>

  <style>
    @keyframes fade-in {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .animate-fade-in { animation: fade-in 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; }

    @keyframes spin-slow {
      from { transform: rotate(0deg) scale(1.1); }
      to { transform: rotate(360deg) scale(1.1); }
    }
    .animate-spin-slow { animation: spin-slow 20s linear infinite; }

    @keyframes spin-slow-reverse {
      from { transform: rotate(360deg) scale(1.02); }
      to { transform: rotate(0deg) scale(1.02); }
    }
    .animate-spin-slow-reverse { animation: spin-slow-reverse 25s linear infinite; }

    @keyframes float {
      0%, 100% { transform: translateY(0) rotate(var(--tw-rotate, 0deg)); }
      50% { transform: translateY(-15px) rotate(var(--tw-rotate, 0deg)); }
    }
    .animate-float { animation: float 6s ease-in-out infinite; }

    .font-handwriting {
      font-family: var(--font-cedarville, cursive);
      font-style: italic;
    }
  </style>
</section>
