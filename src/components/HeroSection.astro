---

---

<section
  class="relative min-h-[85vh] flex items-center bg-slate-950 overflow-hidden"
>
  <!-- Subtle Background Texture -->
  <div class="absolute inset-0 z-0">
    <div
      class="absolute top-[-10%] right-[-5%] w-[600px] h-[600px] bg-teal-900/10 rounded-full blur-[120px] mix-blend-screen animate-pulse-slow"
    >
    </div>
    <div
      class="absolute bottom-[-10%] left-[-10%] w-[500px] h-[500px] bg-indigo-900/10 rounded-full blur-[100px] mix-blend-screen animate-pulse-slow delay-700"
    >
    </div>
    <div
      class="absolute inset-0 bg-[url('/noise.png')] opacity-[0.02] mix-blend-overlay"
    >
    </div>
  </div>

  <div class="max-w-6xl mx-auto px-6 md:px-12 lg:px-16 relative z-10 w-full">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      <!-- Personal Introduction -->
      <div
        class="order-2 lg:order-1 flex flex-col items-center lg:items-start text-center lg:text-left"
      >
        <div
          class="mb-6 inline-flex items-center gap-2 px-4 py-2 rounded-full bg-slate-900/50 border border-slate-800/50 backdrop-blur-sm animate-fade-in opacity-0"
          style="animation-delay: 100ms;"
        >
          <span class="relative flex h-2 w-2">
            <span
              class="animate-ping absolute inline-flex h-full w-full rounded-full bg-teal-400 opacity-75"
            ></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-teal-500"
            ></span>
          </span>
          <span class="text-sm text-slate-400 font-medium"
            >Welcome to my portfolio</span
          >
        </div>

        <h1
          class="text-5xl md:text-6xl lg:text-7xl font-serif text-slate-100 leading-[1.1] mb-6 animate-fade-in opacity-0"
          style="animation-delay: 200ms;"
        >
          Hi, I'm <br />
          <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-teal-300 to-indigo-300"
            >Dr. Muhammad</span
          >
        </h1>

        <p
          class="text-lg md:text-xl text-slate-400 leading-relaxed max-w-xl mb-10 animate-fade-in opacity-0"
          style="animation-delay: 300ms;"
        >
          This isn't just a collection of cases—it's a reflection of my journey,
          my passion for craftsmanship, and my dedication to the patients I
          treat.
        </p>

        <div
          class="flex flex-col sm:flex-row gap-4 w-full sm:w-auto animate-fade-in opacity-0"
          style="animation-delay: 400ms;"
        >
          <a
            href="#cases"
            class="inline-flex items-center justify-center px-8 py-4 bg-teal-600 hover:bg-teal-500 text-white rounded-2xl transition-all duration-300 shadow-lg shadow-teal-900/20 group"
          >
            <span>Explore My Work</span>
            <svg
              class="w-5 h-5 ml-2 transition-transform group-hover:translate-x-1"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </a>
          <a
            href="#about"
            class="inline-flex items-center justify-center px-8 py-4 bg-slate-900/50 hover:bg-slate-800 text-slate-300 border border-slate-800 rounded-2xl transition-all duration-300"
          >
            About Me
          </a>
        </div>
      </div>

      <!-- Hero Portrait / Visual -->
      <div
        class="order-1 lg:order-2 flex justify-center lg:justify-end relative animate-fade-in opacity-0"
        style="animation-delay: 500ms;"
      >
        <div
          class="relative w-72 h-72 md:w-96 md:h-96 lg:w-[450px] lg:h-[450px]"
        >
          <!-- Decorative Rings -->
          <div
            class="absolute inset-0 border-2 border-slate-800 rounded-full scale-[1.02] animate-spin-slow-reverse opacity-30"
          >
          </div>
          <div
            class="absolute inset-0 border border-dashed border-teal-800/30 rounded-full scale-[1.1] animate-spin-slow"
          >
          </div>

          <!-- Image Container -->
          <!-- Abstract Visual Container -->
          <div
            class="absolute inset-0 bg-slate-900 rounded-full overflow-hidden shadow-2xl border-4 border-slate-900 group"
          >
            <!-- Animated Gradient Background -->
            <div class="absolute inset-0 bg-slate-900">
              <div
                class="absolute top-[-50%] left-[-50%] w-[200%] h-[200%] bg-[conic-gradient(from_0deg,transparent_0_340deg,rgba(45,212,191,0.1)_360deg)] animate-[spin_10s_linear_infinite] opacity-50"
              >
              </div>
            </div>

            <!-- Glowing Orbs -->
            <div
              class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full h-full flex items-center justify-center"
            >
              <div
                class="w-[80%] h-[80%] rounded-full bg-gradient-to-tr from-teal-900/40 to-indigo-900/40 blur-2xl animate-pulse"
              >
              </div>
            </div>

            <!-- Geometric Composition -->
            <div class="absolute inset-0 flex items-center justify-center">
              <!-- Glassy Shape -->
              <div
                class="relative w-32 h-32 md:w-48 md:h-48 backdrop-blur-xl bg-white/5 border border-white/10 rounded-2xl shadow-2xl rotate-45 group-hover:rotate-[60deg] transition-transform duration-700 ease-out flex items-center justify-center overflow-hidden"
              >
                <div
                  class="absolute inset-0 bg-gradient-to-br from-transparent via-white/5 to-transparent"
                >
                </div>
                <!-- Core -->
                <div
                  class="w-12 h-12 md:w-16 md:h-16 rounded-full bg-gradient-to-r from-teal-400 to-indigo-500 shadow-[0_0_40px_rgba(45,212,191,0.6)] animate-pulse"
                >
                </div>
              </div>
            </div>

            <!-- Orbital Particles -->
            <div class="absolute inset-0 animate-[spin_8s_linear_infinite]">
              <div
                class="absolute top-10 left-1/2 w-2 h-2 bg-teal-200 rounded-full shadow-[0_0_10px_rgba(45,212,191,1)]"
              >
              </div>
            </div>
            <div
              class="absolute inset-0 animate-[spin_12s_linear_infinite_reverse]"
            >
              <div
                class="absolute bottom-12 right-1/2 w-1.5 h-1.5 bg-indigo-300 rounded-full shadow-[0_0_8px_rgba(99,102,241,1)]"
              >
              </div>
            </div>

            <!-- Noise Overlay -->
            <div
              class="absolute inset-0 bg-[url('/noise.png')] opacity-[0.1] mix-blend-overlay"
            >
            </div>
          </div>

          <!-- Floating "Signature" Note -->
          <div
            class="absolute -bottom-6 -left-4 md:-left-8 bg-slate-900/90 backdrop-blur-md border border-slate-800 p-4 rounded-xl shadow-xl max-w-[200px] animate-float"
          >
            <p class="font-handwriting text-teal-200 text-sm leading-snug">
              "Dentistry is not just a profession, it's a craft."
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    @keyframes fade-in {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .animate-fade-in {
      animation: fade-in 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    }

    @keyframes spin-slow {
      from {
        transform: rotate(0deg) scale(1.1);
      }
      to {
        transform: rotate(360deg) scale(1.1);
      }
    }
    .animate-spin-slow {
      animation: spin-slow 20s linear infinite;
    }

    @keyframes spin-slow-reverse {
      from {
        transform: rotate(360deg) scale(1.02);
      }
      to {
        transform: rotate(0deg) scale(1.02);
      }
    }
    .animate-spin-slow-reverse {
      animation: spin-slow-reverse 25s linear infinite;
    }

    @keyframes float {
      0%,
      100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }
    .animate-float {
      animation: float 5s ease-in-out infinite;
    }

    .font-handwriting {
      font-family: var(--font-cedarville, cursive);
      font-style: italic;
    }
  </style>
</section>
